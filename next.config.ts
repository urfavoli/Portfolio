// your-project/next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Add other Next.js specific configurations here if you need them later,
  // e.g., images: { domains: ['your-image-cdn.com'] }, webpack overrides, etc.
};

export default nextConfig;